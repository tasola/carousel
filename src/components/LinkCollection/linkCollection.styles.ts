const linkWrapper = {
  flexFlow: "row",
  width: "80%",
  margin: "0 auto",
  justifyContent: "space-around",

  "@media (max-width: 576px)": {
    width: "60%",
  }
};

const styles = {
  linkWrapper
};

export default styles;