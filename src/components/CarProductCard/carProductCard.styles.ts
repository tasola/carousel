const productWrapper = {
  margin: "20px",
};

const imageWrapper = {
  width: "100%",
};

const modelTextWrapper = {
  flexFlow: "row",
  marginBottom: "10px"
};

const modelTypeText = {
  marginLeft: "5px",
};

const bodyTypeText = {
  "text-transform": "uppercase",
  // fontSize: "0.8rem",
  // fontWeight: 800
};

const styles = {
  productWrapper,
  imageWrapper,
  modelTextWrapper,
  modelTypeText,
  bodyTypeText
};

export default styles;